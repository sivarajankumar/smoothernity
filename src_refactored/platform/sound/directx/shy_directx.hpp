void shy_platform_sound_directx :: set_listener_position ( so_called_type_platform_vector_data )
{
}

void shy_platform_sound_directx :: set_listener_velocity ( so_called_type_platform_vector_data )
{
}

void shy_platform_sound_directx :: set_listener_orientation ( so_called_type_platform_vector_data , so_called_type_platform_vector_data )
{
}

void shy_platform_sound_directx :: set_sample_value ( so_called_type_platform_sound_sample_mono & , so_called_type_platform_math_num_fract )
{
}

void shy_platform_sound_directx :: create_source ( so_called_type_platform_sound_source_id & )
{
}

void shy_platform_sound_directx :: set_source_pitch ( const so_called_type_platform_sound_source_id & , so_called_type_platform_math_num_fract )
{
}

void shy_platform_sound_directx :: set_source_gain ( const so_called_type_platform_sound_source_id & , so_called_type_platform_math_num_fract )
{
}

void shy_platform_sound_directx :: set_source_position ( const so_called_type_platform_sound_source_id & , so_called_type_platform_vector_data )
{
}

void shy_platform_sound_directx :: set_source_velocity ( const so_called_type_platform_sound_source_id & , so_called_type_platform_vector_data )
{
}

void shy_platform_sound_directx :: set_source_buffer ( const so_called_type_platform_sound_source_id & , so_called_type_platform_sound_buffer_id & )
{
}

void shy_platform_sound_directx :: set_source_playback_looping ( const so_called_type_platform_sound_source_id & )
{
}

void shy_platform_sound_directx :: set_source_playback_once ( const so_called_type_platform_sound_source_id & )
{
}

void shy_platform_sound_directx :: source_play ( const so_called_type_platform_sound_source_id & )
{
}

void shy_platform_sound_directx :: source_stop ( const so_called_type_platform_sound_source_id & )
{
}

void shy_platform_sound_directx :: _create_mono_buffer 
    ( so_called_type_platform_sound_buffer_id & 
    , const so_called_type_platform_sound_sample_mono *
    , so_called_type_platform_math_num_whole
    )
{
}

void shy_platform_sound_directx :: _create_stereo_buffer 
    ( so_called_type_platform_sound_buffer_id &
    , const so_called_type_platform_sound_sample_stereo *
    , so_called_type_platform_math_num_whole
    )
{
}
